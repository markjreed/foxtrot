const std = @import("std");

pub fn main() !void {
    const stdout = std.fs.File.stdout();
    var count: u16 = 1;
    while (count <= 500) : (count += 1) {
        try stdout.writeAll("I will not throw paper airplanes in class.\n");
    }
}
